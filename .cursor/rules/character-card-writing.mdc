---
description: 角色卡写作铁律——强制读模板、禁止改清单；助手只优化模板与流程
alwaysApply: true
---

# 写卡 AI / 本仓库助手边界

## 0. 职责分工（最高优先）

- **模板与流程**（可改）：`基础模板/`、`额外补充包/`、`.cursor/rules/`、`to-do-generator.js`、`config.example.yaml`、`build-card*.js` 等工具链
- **用户作品**（默认只读）：`作品/**`、根目录作品配置如 `*.yaml`（示例配置除外）、`完整作品/**`
- **未经用户明确要求，禁止创建/修改/删除任何具体作品文件**
- 用户若说「改模板/流程」：只动模板与流程；不要「顺便」改作品内容来示范

## 1. 清单只读（写卡时）

- `作品/**/to-do.md` 是需求源，不是草稿。
- **禁止**改写、扩写、删减、重排清单正文。
- 优化模板/流程时：**不要**改作品清单勾选或正文。

## 2. 写前必读模板

仅当用户明确要求写某作品时，开写前必须 **Read** 对应 `基础模板/`：

| 任务 | 必读 |
|------|------|
| 背景 | `Z.1.背景模板.md` |
| 角色 | `Z.2.人物模板.md` 或简要版 |
| 开场白 | `Z.3.开场白.md` |
| 玩家 | `Z.6.玩家模板.md` |
| 文本状态栏 / PHI | `Z.7.*` |
| 系统指令 | `Z.8.系统指令.md` |
| 作品概述 description | `Z.8.作品概述.md` |
| 总规范 | `写卡AI执行规范.md` |

## 3. 标准件只维护在模板区

- 状态栏 / PHI / 系统指令 / 多角色 description 的标准只放在 `基础模板/Z.7*`、`Z.8*`
- 禁止模板教「一字不差输出世界书」或自造 `{{char_xxx}}`
- 是否套用到某作品，由用户或其写卡 AI 自行复制
